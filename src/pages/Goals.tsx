
// In the difficulty rendering section, update the conditional styling
className={cn(
  "px-2 py-0.5 rounded-full text-xs uppercase",
  goal.difficulty === 'hard' ? "bg-red-100 text-red-800" : "bg-gray-200 text-gray-800"
)}
